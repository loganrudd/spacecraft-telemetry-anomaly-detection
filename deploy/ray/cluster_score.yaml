# RayJob — parallel anomaly scoring for all channels on GKE Autopilot.
#
# Environment variables required before applying (cloud_score.sh sets these):
#   PROJECT_ID   GCP project ID
#   REGION       GCP region (e.g. us-central1)
#   MLFLOW_URL   Internal Cloud Run URL for the MLflow tracking server
#   MISSION      Mission name (e.g. ESA-Mission2)
#   TUNED        Set to "1" to fetch and apply tuned_configs.json from GCS.
#                Omit (or set to "") for a clean Hundman-defaults baseline.
#
# Apply via:
#   envsubst < deploy/ray/cluster_score.yaml | kubectl apply -f -
#
# Sizing rationale:
#   Scoring is LSTM inference + pure-numpy anomaly scoring — CPU is sufficient.
#   Workers: 2 vCPU / 4 GiB each, 2 tasks per node (num_cpus=1).
#   Run after cloud-train for the Hundman-defaults baseline (TUNED unset).
#   Run again after cloud-tune with TUNED=1 to produce the held-out comparison.

apiVersion: ray.io/v1
kind: RayJob
metadata:
  name: spacecraft-score
  namespace: ray
spec:
  entrypoint: >-
    bash -c 'set -e;
    if [ "${TUNED}" = "1" ]; then
    python -c "import gcsfs; fs=gcsfs.GCSFileSystem(); src=\"gs://${PROJECT_ID}-artifacts/${MISSION}/tuned_configs.json\"; fs.exists(src) or (print(\"ERROR: tuned_configs.json not found at \" + src) or __import__(\"sys\").exit(1)); fs.get(src, \"/tmp/tuned_configs.json\")";
    spacecraft-telemetry --env cloud ray score
    --mission ${MISSION}
    --channels-from gs://${PROJECT_ID}-processed-data/${MISSION}/channels.txt
    --tuned-configs /tmp/tuned_configs.json;
    else
    spacecraft-telemetry --env cloud ray score
    --mission ${MISSION}
    --channels-from gs://${PROJECT_ID}-processed-data/${MISSION}/channels.txt;
    fi'

  shutdownAfterJobFinishes: true
  ttlSecondsAfterFinished: 3600

  rayClusterSpec:
    rayVersion: "2.31.0"
    enableInTreeAutoscaling: true

    headGroupSpec:
      rayStartParams:
        dashboard-host: "0.0.0.0"
        num-cpus: "0"
      template:
        spec:
          serviceAccountName: ray-sa
          containers:
            - name: ray-head
              image: ${REGION}-docker.pkg.dev/${PROJECT_ID}/spacecraft-telemetry/training:latest
              env:
                - name: SPACECRAFT_ENV
                  value: cloud
                - name: SPACECRAFT_MLFLOW__TRACKING_URI
                  value: ${MLFLOW_URL}
                - name: SPACECRAFT_PREPROCESS__PROCESSED_DATA_DIR
                  value: gs://${PROJECT_ID}-processed-data
                - name: SPACECRAFT_DATA__SAMPLE_DATA_DIR
                  value: gs://${PROJECT_ID}-sample-data
                - name: MLFLOW_ARTIFACTS_DESTINATION
                  value: gs://${PROJECT_ID}-artifacts/mlflow
                - name: SPACECRAFT_RAY__ADDRESS
                  value: auto
                - name: TUNED
                  value: "${TUNED}"
              resources:
                requests:
                  cpu: "1"
                  memory: "2Gi"
                limits:
                  cpu: "1"
                  memory: "2Gi"
              livenessProbe:
                exec:
                  command:
                    - bash
                    - -c
                    - >-
                      wget -T 5 -q -O- http://localhost:52365/api/local_raylet_healthz
                      | grep success &&
                      wget -T 5 -q -O- http://localhost:8265/api/gcs_healthz
                      | grep success
                initialDelaySeconds: 180
                periodSeconds: 30
                timeoutSeconds: 20
                failureThreshold: 5
              readinessProbe:
                exec:
                  command:
                    - bash
                    - -c
                    - >-
                      wget -T 5 -q -O- http://localhost:52365/api/local_raylet_healthz
                      | grep success &&
                      wget -T 5 -q -O- http://localhost:8265/api/gcs_healthz
                      | grep success
                initialDelaySeconds: 90
                periodSeconds: 10
                timeoutSeconds: 20
                failureThreshold: 10

    workerGroupSpecs:
      - groupName: cpu-workers
        replicas: 1
        minReplicas: 0
        # 16 pods × 4 vCPU ÷ num_cpus=1 per task = ~64 concurrent score tasks,
        # so a full 62-channel mission scores in one wave (~10 min). Fewer, larger
        # nodes (vs 31×2vCPU) provision faster on Autopilot and cut per-node Ray
        # daemon overhead. 64 vCPU + head is well under the regional CPUS quota (200).
        maxReplicas: 16
        rayStartParams: {}
        template:
          spec:
            serviceAccountName: ray-sa
            containers:
              - name: ray-worker
                image: ${REGION}-docker.pkg.dev/${PROJECT_ID}/spacecraft-telemetry/training:latest
                env:
                  - name: SPACECRAFT_ENV
                    value: cloud
                  - name: SPACECRAFT_MLFLOW__TRACKING_URI
                    value: ${MLFLOW_URL}
                  - name: SPACECRAFT_PREPROCESS__PROCESSED_DATA_DIR
                    value: gs://${PROJECT_ID}-processed-data
                  - name: SPACECRAFT_DATA__SAMPLE_DATA_DIR
                    value: gs://${PROJECT_ID}-sample-data
                  - name: MLFLOW_ARTIFACTS_DESTINATION
                    value: gs://${PROJECT_ID}-artifacts/mlflow
                  - name: SPACECRAFT_RAY__ADDRESS
                    value: auto
                resources:
                  # 4 vCPU → 4 score tasks/pod (num_cpus=1 each). 4Gi is plenty:
                  # actual per-task usage is ~0.6GiB (mostly fixed torch overhead,
                  # not data-proportional), so 4 tasks ≈ 2.4GiB. 4Gi is also
                  # Autopilot's minimum at 4 vCPU (1 GiB-per-vCPU floor).
                  requests:
                    cpu: "4"
                    memory: "4Gi"
                  limits:
                    cpu: "4"
                    memory: "4Gi"
